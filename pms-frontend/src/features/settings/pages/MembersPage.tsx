import { ComingSoonPage } from "@/shared/components/feedback/ComingSoonPage";

export function MembersPage() {
  return (
    <ComingSoonPage
      title="Thành viên hệ thống"
      description="Quản lý user toàn công ty (khác member trong workspace)."
      moduleName="System Members"
    />
  );
}
